#include<iostream>
#include<string.h>
using namespace std;

f1 = []
f2 = []

for(i=0;i<n;i++)
{
	f1[a[i]] = f[a[i]-1] + a[i] + a[i]*f1[a[i]] + a[i-1]*f2[a[i]]
	f2[i] = f[]
}
int main()
{
	int t;cin>>t;
	while(t--)
	{
		int a,k; char s[100];
		cin>>a>>k>>s;
		if(strlen(s)<3)
			cout<<"1\n"<<endl;
		else
			cout<<"0\n";
	}
	return 0;
}